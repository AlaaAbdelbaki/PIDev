{% extends 'base.html.twig' %}
{% block content %}
    <section class="elements-area mt-30 section-padding-100-0" >
        <div class="container">
            {% for video in videos %}
                <div>testteuvzivbzkv {{ video.url }}</div>
                <iframe width="854" height="480" src="{{video.url}}" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
            {% endfor %}
            <div id="comments" class="row">
                {% include '@FOSComment/Thread/async.html.twig' with {'id': '{{ user.username}}' } %}
            </div>
        </div>
    </section>
{% endblock %}